# Roadmap

质量门禁优先于日历。计划中的两天节奏只是检查机会，不是空发布承诺；一次维护运行至多完成一个版本。

| 版本 | 主题 | 可观察结果 | 关键失败实验 |
|---|---|---|---|
| `v0.1.0` | 约 100 行最小状态图 | 确定性节点与静态边 | 非法图、无限循环预算 |
| `v0.2.0` | 条件路由与循环 | 状态驱动分支和显式终止 | 未知路由、循环越界 |
| `v0.3.0` | 类型化 State 与 Reducer | 更新契约、累积和冲突检测 | 无 reducer 的并发写冲突 |
| `v0.4.0` | 图编译与静态分析 | 可达性、死端、保留节点检查 | 不可达节点和非法边 |
| `v0.5.0` | Channel 与 Superstep | 同一步更新在屏障后可见 | 同步屏障破坏 |
| `v0.6.0` | 并行调度 | 并行节点、确定性提交顺序 | 竞态、部分失败 |
| `v0.7.0` | Checkpoint 与恢复 | thread identity、崩溃后续跑 | 写一半、旧 schema 恢复 |
| `v0.8.0` | Interrupt 与 Command | 批准、拒绝、修改状态后继续 | 重放审批、非法恢复值 |
| `v0.9.0` | Durable Task | retry、timeout、cancel、幂等键 | 重试导致重复副作用 |
| `v0.10.0` | 事件流与回放 | values、updates、trace、replay | 丢事件和顺序错乱 |
| `v0.11.0` | Subgraph | 父子状态适配和命名空间 | 子图状态泄漏 |
| `v0.12.0` | Memory | 会话内 checkpoint 与跨会话 store | 记忆污染和过期数据 |
| `v0.13.0` | 最小 Agent 桥接 | FakeModel、ToolNode、结构化错误 | 模型乱序和工具失败 |
| `v0.14.0` | 多角色编排与 Evals | Planner/Researcher/Reviewer、轨迹评测 | 角色空转和伪协作 |
| `v1.0.0` | Deep Research 生产案例 | 证据图、引用检查、恢复、发布证据 | 网络、模型、解析和人工驳回 |

## 每版必须回答

1. 上一版在哪个可复现实验中失败？
2. 本版新增的最小抽象是什么？
3. 权威状态在哪里，由谁决定下一步？
4. 副作用在何处发生，如何保证幂等、审批和审计？
5. 哪个外部可观察断言证明完成？
6. 与固定 LangGraph 上游的相同点、简化点和自有设计分别是什么？

## v1.0 真实案例验收

- 默认离线 corpus、FakeSearch、FakeModel 可完整运行。
- 真实 Web 和模型是可选 adapter，不改变核心状态机。
- 报告中的事实性结论可追踪到 evidence ID。
- 在指定 checkpoint 杀进程后可以恢复且不重复副作用。
- 人工可以修改研究计划、驳回证据或要求补充检索。
- 固定 fixture 下引用覆盖率、错误引用率和轨迹断言可重复。
- 支持安全边界、迁移策略、可观测、构建、安装和回滚证据。
